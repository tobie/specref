{
  "name": "specref",
  "version": "0.0.5",
  "description": "A list of spec references and related API.",
  "main": "index.js",
  "dependencies": {
    "express": "3.2.4",
    "zlib": "*",
    "connect-cors": "*"
  },
  "devDependencies": {
    "mocha": "*",
    "ent": "*"
  },
  "scripts": {
    "test": "mocha -u tdd ./test/*.js",
    "start": "node index.js"
  },
  "subdomain": "specref",
  "engines": {
    "node": "0.8.x"
  }
}
